{
  "name": "tumaia-workspace",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c cyan,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "check:replicate": "npm run check:replicate --prefix backend",
    "test:replicate:smoke": "npm run test:replicate:smoke --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
