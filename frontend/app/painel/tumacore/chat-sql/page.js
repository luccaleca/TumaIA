import TumaCorePlaceholder from "../TumaCorePlaceholder";

export default function TumaCoreChatSqlPage() {
  return (
    <TumaCorePlaceholder
      title="Chat SQL"
      description="Pergunte em português e receba SQL para inspecionar os dados."
    />
  );
}
